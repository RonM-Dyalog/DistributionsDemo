    XVals←{⎕IO←1
     ErrLeft←'Left Argument must be two numbers in ascending order'
     sink←ErrLeft ⎕Signal(2≠⍴,⍺)/5
     sink←ErrLeft ⎕Signal(0≠1↑0⍴⍺)/11
     sink←ErrLeft ⎕SIGNAL(~</2⍴⍺)/11
     ErrRight←'Right Argument must be a positive whole number > 1'
     sink←ErrRight ⎕SIGNAL(1≠⍴,⍵)/5
     sink←ErrRight ⎕Signal(0≠1↑0⍴⍵)/11
     sink←ErrRight ⎕SIGNAL(1≥items←1⍴⍵)/11
     sink←ErrRight ⎕SIGNAL(items≠⌊items)/11
     (base limit)←,⍺
     base+(limit-base)×(⍳items+1)÷items
    }
