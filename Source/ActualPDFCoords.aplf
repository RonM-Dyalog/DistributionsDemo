 r←Name ActualPDFCoords Parameters;⎕IO;dr;parms;samples;x;cg;y;span
 ⎕IO←1
 dr←DistRef Name
 parms←dr.VerifyControlParameters Parameters
 samples←(dr.RangeSpan Parameters){((⍵≥⍺[1])∧⍵≤⍺[2])/⍵}parms(16808⌶)Name MeshCount
 span←(⌊/,⌈/)samples
 x←span XVals BucketCount
 cg←{⍺(≢⍵)}⌸x⍸samples
 y←(1+⍴x)⍴0
 ⎕IO←0
 y[cg[;0]]←cg[;1]×(⍴x)÷(⍴samples)×--/span
 r←(1↓y)x