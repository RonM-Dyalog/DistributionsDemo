∇ r←Name IBeamPDFValues Parameters;⎕IO;dr;parms;samples;x;cg;y
    ⎕IO←1
    dr←DistRef Name
    parms←dr.VerifyControlParameters Parameters
    samples←parms(16808⌶)Name MeshCount
    x←(dr.RangeSpan parms) XVals BucketCount
    cg←{⍺ (≢⍵)}⌸x⍸samples
    y←(1+⍴x)⍴0
    ⎕IO←0
    y[cg[;0]]←cg[;1]×(⍴x)÷⍴samples
    r←1↓¯2↓y
∇
