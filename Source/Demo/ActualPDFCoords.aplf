 r←Parameters ActualPDFCoords Name;⎕IO;dr;parms;samples;x;cg;y;span
 ⎕IO←1
 dr←DistRef Name
 samples←(dr.RangeSpan Parameters){((⍵≥⍺[1])∧⍵≤⍺[2])/⍵}Parameters dr.Samples MeshCount
 span←(⌊/,⌈/)samples
 x←span XVals BucketCount
 cg←{⍺(≢⍵)}⌸x⍸samples
 y←(1+⍴x)⍴0
 ⎕IO←0
 y[cg[;0]]←cg[;1]×(⍴x)÷(⍴samples)×--/span
 r←(1↓y)x
